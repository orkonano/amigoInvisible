<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/integration"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:mail="http://www.springframework.org/schema/integration/mail"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
              http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
              http://www.springframework.org/schema/integration
              http://www.springframework.org/schema/integration/spring-integration-2.2.xsd
              http://www.springframework.org/schema/integration/mail 
              http://www.springframework.org/schema/integration/mail/spring-integration-mail-2.2.xsd">

	<channel id="app-pipeline-chanel"/>
	<channel id="email-channel-transformer"/>
	<channel id="lista-amigos-channel"/>
	<channel id="lista-amigos-channel-tranformer"/>
	<channel id="outboundMailChannel" />
	
	<gateway id="appPipelineGateway" default-request-channel="app-pipeline-chanel"
			service-interface="orko.dev.amigoInvisible.gateway.AppPipelineGateway"
			default-reply-timeout="5000"/>
			
	<chain input-channel="app-pipeline-chanel" >
		<recipient-list-router default-output-channel="errorChannel">
			<recipient channel="email-channel-transformer" selector-expression="payload.class.simpleName == 'Regalo'"/>
			<recipient channel="lista-amigos-channel-transformer" selector-expression="payload.class.simpleName == 'ListAmigoRecipient'"/>
		</recipient-list-router>
	</chain>
	
	<transformer input-channel="lista-amigos-channel-transformer"	output-channel="lista-amigos-channel" ref="listaAmigoTransformer" method="transform" />
	
	<service-activator input-channel="lista-amigos-channel" method="calcularAmigoInvisible" ref="amigoInvisibleService"/>
	
	<transformer input-channel="email-channel-transformer"	output-channel="outboundMailChannel" ref="emailTransformer" method="transform" />
	
	<mail:outbound-channel-adapter channel="outboundMailChannel" mail-sender="mailSender" />
	
	<beans:bean id="mailMessage" scope="prototype" class="org.springframework.mail.SimpleMailMessage">
		<beans:property name="from" value="${grails.mail.from}" />
		<beans:property name="replyTo" value="${grails.mail.reply}" />
	</beans:bean>
	
	<beans:bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl" >
		<beans:property name="host" value="${grails.mail.host}" />
		<beans:property name="port" value="${grails.mail.port}" />
		<beans:property name="username" value="${grails.mail.username}" />
		<beans:property name="password" value="${grails.mail.password}" />
		<beans:property name="javaMailProperties">
			<beans:props>
				<beans:prop key="mail.smtp.auth">true</beans:prop>
				<beans:prop key="mail.smtp.socketFactory.port">${grails.mail.port}</beans:prop>
				<beans:prop key="mail.smtp.socketFactory.class">javax.net.ssl.SSLSocketFactory</beans:prop>
				<beans:prop key="mail.smtp.socketFactory.fallback">false</beans:prop>
			</beans:props>
		</beans:property>
	</beans:bean>
	
</beans:beans>